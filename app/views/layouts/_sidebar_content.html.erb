<div class="border-top py-2" >
  <div class="small mb-1 pl-1">
    ■<%= top_beer.beer_style.name %>部門
  </div>
  <div class="d-flex">
    <div class="d-inline-box">
      <span class="fa-layers mt-2">
        <i class="fas fa-crown fa-2x text-warning", data-fa-transform="up-3"></i>
        <span class="fa-layers-text font-weight-bold", data-fa-transform="right-11 up-3">
          1
        </span>
      </span>
    </div>
    <div class="d-inline-box text-middle ml-4">
      <%= link_to top_beer.name, beer_path(top_beer), class: "text-reset" %>
    </div>
  </div>
  <div class="d-flex align-items-end border-bottoms">
    <% if top_beer.evaluation == nil %>
      <span>未評価</span>
    <% else %>
      <span class="beers-rate-sidebar<%= top_beer.id %>">
        <%= render "public/beers/beer_rate_display", beer: top_beer %>
      </span>
      <span class="ml-2 font-weight-bold ">
        <%= top_beer.evaluation.round(2) %>
      </span>
    <% end %>
  </div>
</div>

